# 项目约定

## 项目定位

- 这是一个面向个人生活与小型经营场景的工具工作台，核心模块包括咖啡摊经营、行程规划、事务管理和首页总览。
- 技术栈为 Vue 3、Vite、TypeScript、Element Plus、Pinia；使用 `pnpm`，不要改用 npm 或 yarn。
- 用户界面、提示语和业务文档默认使用简体中文；代码标识符保持英文，并优先沿用现有命名。

## 目录与实现约定

- 页面放在 `src/views`，接口和领域类型放在 `src/api`，跨页面状态放在 `src/stores`，通用能力放在 `src/components`、`src/composables` 或 `src/utils`。
- 使用 `@/` 路径别名。优先复用已有组件、组合式函数、API 封装和 Element Plus 交互模式。
- 菜单由 Mock 接口动态提供。新增或调整可访问页面时，同时检查视图文件、`mock/menu.mock.ts`、路由名称、组件路径和 `keepAlive` 配置是否一致。
- 自动导入的类型声明生成在 Vite 配置中被有意关闭；除非任务明确要求，不要重新开启或生成相关声明文件。
- 保持改动聚焦于当前需求，不顺带清理模板代码、批量重排目录或重写无关样式。

## 数据与业务安全

- `mock/data/*.json` 保存事务和行程数据，开发环境中的 PUT 操作会直接写入这些文件；它们应视为真实用户数据，而不是可随意重置的测试夹具。
- `mock/business` 保存商品、订单、预定和库存流水。业务写入应继续通过现有 Mock API 及事务式持久化逻辑完成，不要从页面直接改 JSON，也不要绕过库存流水。
- 修改数据结构时必须同步更新 TypeScript 类型、默认值、读取兼容和写入逻辑；已有 JSON 缺少新增字段时应有安全默认值，避免要求用户手工迁移。
- 不覆盖、删除或批量格式化现有业务数据，除非用户明确要求。写入前保留未知字段，失败时不得留下部分更新的数据。
- 咖啡摊业务必须遵守 `src/views/business/FEATURES.md`：禁止负库存，作废订单只退回一次库存，预定只锁定可售库存，作废订单不计入收入和销量。
- 金额、数量、日期和状态变化必须在领域逻辑中校验；历史订单中的商品名称与价格保持下单时快照。
- 不提交或展示 `.env*` 中的凭据、令牌和私密配置。

## 工作方式

- 开始前先查看 `git status`，阅读相关页面、API、类型、Mock 处理器和模块文档；保留用户已有改动，不覆盖或回退无关文件。
- 对跨模块功能先明确数据源、保存位置、兼容策略和验收标准，再实施最小可用的纵向闭环。
- 修复问题时优先解决根因，并为加载中、空数据、失败、重复点击和非法输入提供与现有页面一致的反馈。
- 不引入新依赖，除非现有依赖无法合理完成需求；新增依赖前说明必要性和影响。
- 不执行提交、推送、发布或破坏性 Git 操作，除非用户明确要求。

## 验证要求

- 代码改动至少运行 `pnpm type-check`。
- 涉及路由、Vite 配置、依赖、构建行为或跨模块改动时，运行 `pnpm build`。
- 回归受影响的核心流程；经营模块重点验证下单扣库存、作废退库存、预定锁定和统计口径。
- 现有 `pnpm lint` 会自动改写文件，不要把它作为只读检查使用，也不要对整个仓库进行无关格式化。需要时仅检查或格式化本次改动文件。
- 仓库当前没有自动化测试脚本。新增复杂纯逻辑时优先拆成可测试函数；如果任务引入测试框架，应保持配置轻量并补充对应脚本。

## 交付说明

- 完成后简要说明行为变化、改动文件、已运行的验证及未验证项。
- 若发现与当前任务无关的脏文件或潜在问题，只报告，不擅自修改。
